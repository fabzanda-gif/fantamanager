"use client";import Link from"next/link";import brand90 from"../assets/logos/SidebarPNG.png";import statsBg from"../assets/ambience/Statistiche.jpeg";import{useEffect,useState}from"react";import{CalendarDays,Users,Activity,BarChart3}from"lucide-react";import{createClient}from"@supabase/supabase-js";const sb=createClient("https://fqngllsmfqatgwzezuus.supabase.co","sb_publishable_JfPi6jdFfg8l51Z7SG3IYw_kK5J0x4L");export default function SeasonStats(){const[run,setRun]=useState(""),[rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const id=new URLSearchParams(location.search).get("run")||"";setRun(id);const r=await sb.from("game_player_season_stats").select("*").eq("run_id",id);setRows((r.data||[]).sort((a:any,b:any)=>(b.rating_sum/Math.max(1,b.appearances))-(a.rating_sum/Math.max(1,a.appearances))))})()},[]);return <main className="seasonPage ambienceSection" style={{backgroundImage:`linear-gradient(rgba(4,12,9,.84),rgba(4,12,9,.95)),url(${statsBg.src})`}}><aside className="seasonNav"><div className="seasonBrand"><img src={brand90.src} alt="90 MINUTES"/></div><b>STAGIONE</b><Link href={"/stagione?run="+run}><BarChart3/> Hub</Link><Link href={"/rosa?run="+run}><Users/> Rosa</Link><Link href={"/calendario?run="+run}><CalendarDays/> Calendario</Link><Link href={"/classifica?run="+run}><Activity/> Classifica</Link><Link className="active" href={"/statistiche-stagione?run="+run}><BarChart3/> Statistiche</Link><small>ARCHIVIO</small><span>Movimenti</span><span>Report</span></aside><section className="matchStatsPage seasonSectionMain"><div className="newTop"><span className="matrixRun">DATA // STAGIONE</span></div><header className="prepHero"><p className="eyebrow">PERFORMANCE // ARCHIVIO</p><h1>Statistiche stagionali.</h1><p>Produzione, continuità e rendimento accumulati partita dopo partita.</p></header><section className="playerData seasonData"><div className="sectionTitle">GIOCATORI</div>{rows.map(r=><article key={r.player_id}><strong>{r.player_name}</strong><span>{(r.rating_sum/Math.max(1,r.appearances)).toFixed(1)}</span><small>{r.appearances} PRES · {r.goals} GOL · {r.assists} ASSIST · {Number(r.xg).toFixed(2)} xG {r.saves?" · "+r.saves+" PARATE · "+r.clean_sheets+" CLEAN SHEET":""}</small></article>)}</section></section></main>}