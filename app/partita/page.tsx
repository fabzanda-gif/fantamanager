"use client";
import Link from"next/link";import{useEffect,useRef,useState}from"react";import{ArrowLeft,Play,Radio,Trophy}from"lucide-react";import{createClient}from"@supabase/supabase-js";
const sb=createClient("https://fqngllsmfqatgwzezuus.supabase.co","sb_publishable_JfPi6jdFfg8l51Z7SG3IYw_kK5J0x4L");
type P={player_id:string;player_name:string;role:string;confidence:number};
function seeded(s:string){let h=2166136261;for(const c of s)h=Math.imul(h^c.charCodeAt(0),16777619);return()=>((h=Math.imul(h^(h>>>15),2246822519))>>>0)/4294967296}
function SevenDigit({value}:{value:number}){const n=Math.max(0,Math.min(9,value)),map=["abcdef","bc","abdeg","abcdg","bcfg","acdfg","acdefg","abc","abcdefg","abcdfg"],on=map[n];return <span className="sevenDigit">{["a","b","c","d","e","f","g"].map(s=><i key={s} className={"seg seg-"+s+(on.includes(s)?" on":"")}/>)}</span>}
export default function Partita(){const[run,setRun]=useState(""),[round,setRound]=useState(1),[club,setClub]=useState(""),[opp,setOpp]=useState(""),[venue,setVenue]=useState("home"),[style,setStyle]=useState("Equilibrato"),[ps,setPs]=useState<P[]>([]),[minute,setMinute]=useState(0),[events,setEvents]=useState<any[]>([]),[score,setScore]=useState([0,0]),[playing,setPlaying]=useState(false),[finished,setFinished]=useState(false);const plan=useRef<any[]>([]);
useEffect(()=>{(async()=>{const q=new URLSearchParams(location.search),id=q.get("run")||"",rn=Number(q.get("round")||1);setRun(id);setRound(rn);const r=await sb.from("game_runs").select("club_code").eq("id",id).single();setClub(r.data?.club_code||"");const mp=await sb.from("game_match_plans").select("*").eq("run_id",id).eq("round",rn).single();if(mp.data){setOpp(mp.data.opponent_code);setVenue(mp.data.venue);setStyle(mp.data.style)}const old=await sb.from("game_matches").select("user_goals,opponent_goals,status").eq("run_id",id).eq("round",rn).maybeSingle();if(old.data?.status==="finished"){setScore([old.data.user_goals,old.data.opponent_goals]);setMinute(90);setFinished(true)}const li=await sb.from("game_lineups").select("id").eq("run_id",id).single();if(li.data){const lp=await sb.from("game_lineup_players").select("player_id").eq("lineup_id",li.data.id),ids=(lp.data||[]).map((x:any)=>x.player_id);if(ids.length){const p=await sb.from("game_run_players").select("player_id,player_name,role,confidence").eq("run_id",id).in("player_id",ids);setPs(p.data||[])}}})()},[]);
function build(){const rnd=seeded(run+"-g"+round+"-"+style),out:any[]=[];const styleChance:any={Equilibrato:.50,Possesso:.51,Verticale:.53,Contropiede:.52,Pressing:.54};for(let m=5;m<90;m+=3+Math.floor(rnd()*8)){const us=rnd()<styleChance[style];if(rnd()<.48)continue;const p=us?ps[Math.floor(rnd()*ps.length)]:null,roll=rnd();if(roll<.16)out.push({minute:m,type:"goal",side:us?"user":"opp",player:p,desc:us?`GOL! ${p?.player_name} trova lo spazio e conclude l'azione.`:`Gol ${opp}. La difesa non riesce a chiudere in tempo.`});else if(roll<.28)out.push({minute:m,type:"chance",side:us?"user":"opp",player:p,desc:us?`${p?.player_name} crea una buona occasione, ma manca l'ultimo gesto.`:`${opp} arriva alla conclusione: pericolo scampato.`});else out.push({minute:m,type:"action",side:us?"user":"opp",player:p,desc:us?`${p?.player_name}: buona lettura, recupero e avanzamento dell'azione.`:`${opp} prova a consolidare il possesso.`})}plan.current=out}
async function start(){if(!ps.length)return;build();setPlaying(true);let i=0;const t=setInterval(()=>{i+=2;setMinute(Math.min(i,90));const now=plan.current.filter(e=>e.minute<=i);setEvents(now);setScore([now.filter(e=>e.type==="goal"&&e.side==="user").length,now.filter(e=>e.type==="goal"&&e.side==="opp").length]);if(i>=90){clearInterval(t);setPlaying(false);setFinished(true);void persist(now)}},110)}
async function persist(ev:any[]){const ug=ev.filter(e=>e.type==="goal"&&e.side==="user").length,og=ev.filter(e=>e.type==="goal"&&e.side==="opp").length;const m=await sb.from("game_matches").upsert({run_id:run,round,opponent_code:opp,venue,style,user_goals:ug,opponent_goals:og,status:"finished"},{onConflict:"run_id,round"}).select("id").single();if(!m.data)return;const mid=m.data.id;await sb.from("game_match_events").delete().eq("match_id",mid);if(ev.length)await sb.from("game_match_events").insert(ev.map(e=>({match_id:mid,minute:e.minute,event_type:e.type,team_side:e.side,player_id:e.player?.player_id||null,player_name:e.player?.player_name||null,description:e.desc})));const home=venue==="home"?club:opp,away=venue==="home"?opp:club,hg=venue==="home"?ug:og,ag=venue==="home"?og:ug;await sb.from("game_season_results").upsert({run_id:run,round,home_code:home,away_code:away,home_goals:hg,away_goals:ag},{onConflict:"run_id,round,home_code,away_code"});await sb.from("game_runs").update({current_round:round}).eq("id",run)}
return <main className="livePage"><div className="newTop"><Link href={"/vigilia?run="+run+"&round="+round} className="back"><ArrowLeft size={17}/> Vigilia</Link><span className="matrixRun">LIVE // GIORNATA {String(round).padStart(2,"0")}</span></div><div className="scoreboard"><div className="scoreTop"><span>{round}ª GIORNATA</span><strong>{finished?"● FINALE":minute+"'"}</strong></div><div className="scoreBody"><div className="scoreTeam home"><small>CASA</small><b>{venue==="home"?club:opp}</b></div><div className="digitalScore"><div className="segmentScore"><SevenDigit value={venue==="home"?score[0]:score[1]}/><span className="segmentDash"/><SevenDigit value={venue==="home"?score[1]:score[0]}/></div></div><div className="scoreTeam away"><small>OSPITI</small><b>{venue==="home"?opp:club}</b></div></div></div><div className="liveGrid"><section className="commentary"><div className="liveHead"><Radio size={15}/> CRONACA LIVE</div>{events.length===0?<p className="waiting">Lo stadio aspetta il calcio d'inizio.</p>:events.slice().reverse().map((e,i)=><article key={i} className={e.type==="goal"?"goalEvent":""}><b>{e.minute}'</b><p>{e.desc}</p></article>)}</section><aside className="liveSide"><h3>PIANO PARTITA</h3><strong>{style}</strong>{!playing&&!finished&&<button onClick={start}><Play size={16}/> CALCIO D'INIZIO</button>}{playing&&<div className="simPulse">SIMULAZIONE IN CORSO</div>}{finished&&<div className="fullTime"><Trophy size={20}/><b>90' — FINE</b><span>Giornata {round} salvata.</span><Link href={"/report?run="+run+"&round="+round}>REPORT PARTITA →</Link><Link href={"/stagione?run="+run}>TORNA ALLA STAGIONE →</Link></div>}</aside></div></main>}