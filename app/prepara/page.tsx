"use client";
import Link from "next/link";import{useEffect,useMemo,useState}from"react";import{ArrowLeft,Check,Crown,Shield}from"lucide-react";import{createClient}from"@supabase/supabase-js";
const sb=createClient("https://fqngllsmfqatgwzezuus.supabase.co","sb_publishable_JfPi6jdFfg8l51Z7SG3IYw_kK5J0x4L");
type P={player_id:string;player_name:string;role:string;source_club:string;confidence:number};const shapes:any={"4-3-3":["P","D","D","D","D","C","C","C","A","A","A"],"4-4-2":["P","D","D","D","D","C","C","C","C","A","A"],"3-5-2":["P","D","D","D","C","C","C","C","C","A","A"],"4-2-3-1":["P","D","D","D","D","C","C","C","C","C","A"],"3-4-3":["P","D","D","D","C","C","C","C","A","A","A"]};
export default function Prepara(){const[ps,setPs]=useState<P[]>([]),[form,setForm]=useState("4-3-3"),[xi,setXi]=useState<string[]>([]),[capt,setCapt]=useState(""),[run,setRun]=useState(""),[saved,setSaved]=useState(false),[busy,setBusy]=useState(true);useEffect(()=>{(async()=>{const id=new URLSearchParams(location.search).get("run")||"";setRun(id);const p=await sb.from("game_run_players").select("player_id,player_name,role,source_club,confidence").eq("run_id",id).eq("squad_status","accepted");setPs(p.data||[]);setBusy(false)})()},[]);const needs=useMemo(()=>shapes[form].reduce((a:any,r:string)=>(a[r]=(a[r]||0)+1,a),{}),[form]);function toggle(p:P){setSaved(false);setXi(x=>x.includes(p.player_id)?x.filter(i=>i!==p.player_id):(x.filter(id=>ps.find(q=>q.player_id===id)?.role===p.role).length<(needs[p.role]||0)?[...x,p.player_id]:x))}async function save(){if(xi.length!==11||!capt)return;setBusy(true);const old=await sb.from("game_lineups").select("id").eq("run_id",run).maybeSingle();let lid=old.data?.id;if(lid){await sb.from("game_lineup_players").delete().eq("lineup_id",lid);await sb.from("game_lineups").update({formation:form,captain_player_id:capt,updated_at:new Date().toISOString()}).eq("id",lid)}else{const n=await sb.from("game_lineups").insert({run_id:run,formation:form,captain_player_id:capt}).select("id").single();lid=n.data?.id}if(lid){const counters:any={};await sb.from("game_lineup_players").insert(xi.map(id=>{const r=ps.find(p=>p.player_id===id)!.role;counters[r]=(counters[r]||0)+1;return{lineup_id:lid,player_id:id,slot_code:r+counters[r]}}));setSaved(true)}setBusy(false)}return <main className="prepPage"><div className="newTop"><Link href={"/rosa?run="+run} className="back"><ArrowLeft size={17}/> Rosa</Link><span className="matrixRun">PREPARAZIONE</span></div><header className="prepHero"><p className="eyebrow">FASE 04 // SPOGLIATOIO</p><h1>Dagli una forma.</h1><p>La Matrice ti ha dato i giocatori. Ora scegli come metterli insieme.</p></header><div className="prepGrid"><section className="prepPanel"><h3>MODULO</h3><div className="formationTabs">{Object.keys(shapes).map(f=><button className={f===form?"active":""} onClick={()=>{setForm(f);setXi([]);setCapt("");setSaved(false)}} key={f}>{f}</button>)}</div><div className="pitch"><div className="pitchScore">{xi.length}/11</div>{["A","C","D","P"].map(r=><div className="pitchLine" key={r}>{Array.from({length:needs[r]||0}).map((_,i)=>{const p=ps.filter(p=>p.role===r&&xi.includes(p.player_id))[i];return <div className={"pitchSlot "+(p?"filled":"")} key={i}><span>{p?p.player_name:"+"}</span>{p&&<small>{p.confidence}%</small>}</div>})}</div>)}</div></section><section className="prepPanel rosterPick"><h3>ROSA <span>scegli l'XI</span></h3>{["P","D","C","A"].map(r=><div key={r} className="pickRole"><b>{r}</b>{ps.filter(p=>p.role===r).map(p=><button key={p.player_id} onClick={()=>toggle(p)} className={xi.includes(p.player_id)?"picked":""}><span>{p.player_name}<small>{p.source_club}</small></span><em>{p.confidence}%</em>{xi.includes(p.player_id)&&<Check size={14}/>}</button>)}</div>)}</section></div><section className="captainBar"><Crown size={18}/><div><strong>CAPITANO</strong><span>Prima gerarchia dello spogliatoio</span></div><select value={capt} onChange={e=>{setCapt(e.target.value);setSaved(false)}}><option value="">Scegli...</option>{ps.filter(p=>xi.includes(p.player_id)).map(p=><option value={p.player_id} key={p.player_id}>{p.player_name}</option>)}</select><button disabled={busy||xi.length!==11||!capt} onClick={save}><Shield size={16}/>{saved?"FORMAZIONE SALVATA":"SALVA XI E GERARCHIA"}</button></section>{saved&&<div className="goMatch"><Link href={"/vigilia?run="+run}>VAI ALLA VIGILIA →</Link></div>}</main>}